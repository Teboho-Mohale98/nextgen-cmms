{
  "name": "nextgen-cmms-functions",
  "private": true,
  "version": "0.1.0",
  "description": "Cloud Functions for NextGen CMMS: RBAC, AI analysis, IoT trigger engine, scheduler.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "~5.7.3"
  }
}